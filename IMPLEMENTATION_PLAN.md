# План развития Nutag

Документ является рабочим roadmap для локального приложения учёта домашнего производства замороженных полуфабрикатов. Он не заменяет функциональную постановку из `README_FUNCTIONAL.md`, `01_scope_and_principles.md`, `02_registers_and_entities.md`, `03_calculation_rules.md` и `04_workflows_and_controls.md`, но фиксирует текущее состояние реализации, принятые уточнения, отказанные решения и следующий шаг.

## 0. Правила сопровождения плана

- План должен показывать, что уже сделано, что осталось, от чего отказались и какой следующий шаг.
- Активные решения не должны противоречить deprecated/rejected разделу.
- У backlog-пунктов должен быть явный статус: `done`, `partial`, `current`, `next`, `not started`, `blocked`, `rejected`, `legacy only`, `not in MVP`.
- В разделе "Следующий шаг" должен быть ровно один главный следующий шаг.
- Основная последовательность доработок и UI-проверки: `Закупки -> Заготовки -> Производство -> Заказы`.
- Доработки более поздних этапов допустимы для защиты сервисных инвариантов, но пользовательское UI-тестирование не нужно переносить на поздний этап, пока предыдущий этап не стабилизирован.
- Защита от ошибок ввода должна рассматриваться отдельно для каждой формы. Перед реализацией по каждой форме нужно дать пользователю краткое описание предлагаемой защиты и получить отдельное подтверждение; без такого подтверждения реализацию не начинать.

## 1. Текущее состояние

### 1.1. Реализовано

- `done` Каркас локального Streamlit-приложения с многостраничной структурой.
- `done` SQLite + SQLAlchemy + Alembic.
- `done` Базовые справочники: единицы измерения, ингредиенты, продукты, виды заготовок, упаковка, расходники, оборудование, постоянные расходы, ставка труда.
- `done` Закупки с позициями ингредиентов, упаковки и расходников.
- `done` Сервисная валидация строк закупки: ровно один источник должен соответствовать `item_type`.
- `partial` Складские остатки: есть агрегированные остатки и список доступных партий закупок/заготовок.
- `done` Сервисная проверка выбранных партий закупок и заготовок при создании заготовок и производственных партий.
- `done` Цена списания выбранной партии берётся из источника, а не из UI.
- `partial` Заготовки: создание, себестоимость, выбранные партии ингредиентов, остаток заготовок.
- `partial` Производственные партии: создание, себестоимость, выбранные партии ингредиентов/упаковки/заготовок, фасованный выход.
- `partial` Заказы: базовое создание заказов и позиций, сервисная проверка и UI-выбор партии готовой продукции находятся в рабочей копии.
- `partial` Экономика: базовая маржа и отчётность по заказам.
- `partial` Экспорт: остатки, заказы, производство.
- `partial` Контрольные сигналы: отрицательные остатки и базовые проблемы данных.
- `partial` Русская локализация: основные UI-строки, типы и статусы переведены, но отдельные внутренние сообщения и старые формулировки ещё требуют ревизии.

### 1.2. Проверка

- Команда проверки: `.venv\Scripts\python.exe -m pytest`.
- Текущее состояние автотестов: `49 passed`.
- Покрыто тестами: расчёты, БД, закупки, партийные остатки, заготовки, производство, заказы, локализация enum/status.
- Не покрыто автотестами: Streamlit UI, ручные сценарии редактирования, полноценные отчёты.

## 2. Принятые продуктовые решения

- Учёт запасов должен быть партийным, а не кумулятивным средневзвешенным.
- При списании пользователь выбирает конкретную доступную закупку или заготовку.
- Сервисный слой обязан проверять выбранный источник, тип позиции, единицу измерения и достаточный остаток.
- Если выбран источник списания, цена берётся из выбранной партии, а не из значений UI.
- Остатки должны быть трассируемыми на уровне партий для ингредиентов, упаковки, расходников, заготовок и готовой продукции.
- Legacy reconciliation должен уважать границы типов сущностей и не должен сопоставлять записи только по совпадающим numeric ID.
- Все пользовательские UI-строки, типы, статусы и сообщения должны быть на русском языке.
- Амортизация оборудования должна распределяться по объёму производства, а не вводиться вручную в каждую партию как произвольная сумма.
- Реальное списание запасов после фиксации заготовки или производства предпочтительнее отложенной сверки.
- Пользовательская проверка должна идти по рабочему сценарию: сначала стабилизируются закупки, затем заготовки, затем производство, затем заказы.

## 3. Устаревшие или отклонённые решения

- `rejected` Средневзвешенная цена больше не является целевым методом складского учёта.
- `legacy only` Функция расчёта средневзвешенной цены и связанные тесты могут временно оставаться для старых отчётов или совместимости, но не должны определять новую складскую логику.
- `rejected` Ручной ввод цены списания в UI не является источником истины, если выбрана партия.
- `rejected` Списание в минус без явного сценария покрытия дефицита не допускается.
- `rejected` Сопоставление остатков по совпадающим ID без проверки типа сущности не допускается.
- `not in MVP` Регистрация, авторизация, серверная инфраструктура и многопользовательская работа.
- `not in MVP` Импорт данных из Excel.

## 4. Архитектурные принципы

- Streamlit-страницы отвечают за ввод и отображение, но не за бизнес-инварианты.
- Бизнес-инварианты должны жить в `nutag/services` и покрываться тестами.
- Модели БД должны описывать состояние и связи, но не должны содержать UI-логику.
- Миграции должны отражать изменения схемы, а не подменяться ручными правками SQLite.
- Складская логика должна быть тестируемой без Streamlit.
- После изменения бизнес-логики нужно запускать `.venv\Scripts\python.exe -m pytest`.
- Повторяющееся создание `engine` / `SessionLocal` / `initialize_database` в Streamlit-страницах нужно вынести в общий helper.
- `inventory.py` нужно постепенно разделять на явные операции: список партий, остаток конкретной партии, проверка списания, агрегированные отчёты, legacy reconciliation.

## 5. Последовательный roadmap

### 5.1. Этап 1: Закупки

Цель этапа: закупки должны быть удобным и корректным источником партийного склада.

- `done` Создание закупки с позициями ингредиентов, упаковки и расходников.
- `done` Сервисная валидация строки закупки: ровно один источник соответствует `item_type`.
- `done` Закупочные строки участвуют в списке доступных партий.
- `partial` Агрегированные остатки по закупкам.
- `partial` Редактирование прошлых закупок: можно исправить дату, поставщика, закупившего, транспорт, комментарий, количество и цену строк у неиспользованных закупок; изменение состава строк пока не реализовано.
- `partial` Удаление ошибочных закупок: можно удалить только неиспользованную закупку с подтверждением в UI.
- `done` Защита от изменения/удаления закупочной партии, уже использованной в заготовке или производстве, реализована в сервисном слое и UI.
- `done` Ввод цены закупки как "за единицу" или "общая сумма" с автоматическим пересчётом в форме новой закупки.
- `done` Защита формы `Новая закупка` от ошибок ввода: проверка заполненности каждой активной строки, положительного количества, неотрицательной цены, согласованности цены за единицу и общей суммы, автоподстановки единицы из справочника, запрета сохранения без строк, предупреждения о незаполненных строках и очистка формы после успешного сохранения.
- `done` Защита формы `История закупок -> редактирование закупки`: проверка положительного количества, неотрицательной цены, корректной даты, блокировка изменения использованных партий, понятное предупреждение при невозможности редактирования и явное отображение пересчитанного итога строки.
- `done` Защита формы `История закупок -> удаление закупки`: текстовое подтверждение удаления, блокировка удаления использованных партий, предупреждение о влиянии на остатки и понятное сообщение после отказа, ошибки или успешного удаления.
- `done` Управляемое количество строк в форме закупки: пользователь может добавлять строки и удалять последнюю строку.

### 5.2. Этап 2: Заготовки

Цель этапа: заготовка должна корректно списывать выбранные партии ингредиентов и создавать собственный трассируемый остаток.

- `done` Создание заготовки.
- `done` Сервисная проверка выбранных партий ингредиентов.
- `done` Цена ингредиента берётся из выбранной партии закупки.
- `partial` Остаток заготовок доступен для производства.
- `done` После успешного сохранения заготовки форма очищается, чтобы не провоцировать задвоение.
- `done` Отходы учитываются в разрезе ингредиентов заготовки: для каждой строки расхода фиксируется количество отходов, отходы не уменьшают складское списание и остаются частью себестоимости годного выхода; общее поле отходов убрано из формы новой заготовки.
- `done` Единица измерения в форме `Заготовки` проставляется автоматически исходя из выбранного ингредиента и не требует ручного выбора; партия дополнительно проверяется на совпадение единицы.
- `done` Корректировка заготовок: неиспользованную заготовку можно изменить с повторной сервисной проверкой выбранных партий, остатков, единиц и пересчётом себестоимости; заготовка, уже использованная в производстве, заблокирована для редактирования.
- `done` Удаление ошибочных заготовок: неиспользованную заготовку можно удалить с текстовым подтверждением; заготовка, уже использованная в производстве, заблокирована для удаления.
- `done` Удобная проверка доступных партий ингредиентов в UI заготовок при большом количестве остатков: пользователь сначала выбирает ингредиент, затем видит только его доступные партии; конкретная партия по-прежнему выбирается явно.
- `done` Защита формы `Заготовки` от ошибок ввода: обязательный вид заготовки, положительный выход, выбранная партия ингредиента для каждой активной строки, достаточный остаток выбранной партии, совпадение единицы измерения, запрет неполных/нулевых строк, очистка формы после сохранения и защита от повторного сохранения.

### 5.3. Этап 3: Производство

Цель этапа: производство должно корректно списывать выбранные партии ингредиентов/упаковки/заготовок и создавать трассируемую готовую продукцию.

- `done` Создание производственной партии.
- `done` Сервисная проверка выбранных партий ингредиентов, упаковки и заготовок.
- `done` Цена ингредиента/упаковки/заготовки берётся из выбранного источника, а не из UI.
- `partial` Фасованный выход готовой продукции сохраняется как `FinishedProductOutput`.
- `done` На странице `Производство` вместо поля `Цена` для ингредиентов отображается стоимость строки с учётом введённого количества.
- `done` На странице `Производство` стоимость строки ингредиента пересчитывается на лету при изменении количества.
- `not started` На странице `Производство` при выборе упаковки единица измерения должна проставляться автоматически.
- `not started` Нефасованная готовая продукция как отдельный складской остаток.
- `not started` Учёт отходов в производстве, если отходы возникают не только на этапе заготовок.
- `not started` Корректировка производственных партий.
- `not started` Удаление ошибочных производственных партий с учётом последующих заказов.
- `approval required` Защита формы `Производство` от ошибок ввода: обязательный продукт, положительный выпуск, выбранные партии ингредиентов/упаковки/заготовок, достаточные остатки, автоподстановка единицы упаковки, пересчёт стоимости ингредиента на лету по количеству, проверка фасовки и суммарного выхода, защита от повторного сохранения.

### 5.4. Этап 4: Заказы

Цель этапа: заказы должны списывать или резервировать конкретные партии готовой продукции без нарушения остатков.

- `partial` Базовое создание заказов и позиций.
- `done in working tree` Сервисная проверка выбранного `FinishedProductOutput` при создании заказа.
- `done in working tree` UI-выбор партии готовой продукции в форме нового заказа.
- `partial` Расчёт себестоимости проданного по выбранной партии готовой продукции: связь с партией есть, отчёты ещё не используют её полноценно.
- `not started` Проверка резерва готовой продукции как отдельного состояния.
- `not started` Частичное резервирование заказа.
- `not started` Снятие резерва.
- `not started` Возврат/отмена заказа с восстановлением доступного остатка.
- `approval required` Защита формы `Заказы` от ошибок ввода: обязательные данные заказа, выбранная партия готовой продукции для каждой строки, положительное количество, достаточный остаток партии, неотрицательная цена продажи, предупреждение о неполных строках и защита от повторного сохранения.
- `later` UI-тестирование заказов возвращается после стабилизации закупок, заготовок и производства.

### 5.5. Этап 5: Экономика, отчёты и экспорт

Цель этапа: отчёты должны использовать трассируемые партии и показывать реальную себестоимость.

- `partial` Расчёт себестоимости заготовок.
- `partial` Расчёт себестоимости производственных партий.
- `partial` Маржа заказов.
- `partial` Постоянные расходы.
- `partial` Экспорт остатков, заказов и производства.
- `not started` Амортизация оборудования по объёму производства.
- `not started` Точка безубыточности.
- `not started` Окупаемость оборудования.
- `not started` Отчёт по личному потреблению.
- `not started` Полный отчёт по периоду.
- `not started` Отчёт по движению конкретной партии, включая закупочные партии.
- `not started` Отчёт по дефицитам и списаниям.
- `not started` Учёт домашних нужд и домашних запасов: списание ингредиентов, заготовок и готовой продукции на личное потребление; оприходование ингредиентов и заготовок из домашних запасов отдельным источником с явной пометкой и трассируемостью.

### 5.6. Сквозные улучшения UI и инфраструктуры

- `partial` Многостраничный Streamlit UI.
- `partial` Русская локализация интерфейса.
- `not started` Общий helper для подключения к БД и инициализации приложения.
- `not started` Автоподстановка базовой единицы измерения из справочника.
- `not started` Настройка точности отображения по единицам измерения.
- `not started` Редактирование справочников.
- `not started` Улучшение выбора партий в UI при большом количестве доступных остатков.
- `not started` Везде, где пользователь указывает трудозатраты или другое время в часах, UI должен поддерживать ввод часов и минут, а расчёты должны корректно переводить это в доли часа.
- `not started` Полная ревизия пользовательских сообщений на русском языке.
- `approval required` Защита форм справочников `Единицы`, `Ингредиенты`, `Продукты`, `Виды заготовок`, `Упаковка`, `Расходники`: обязательное название, защита от дублей с учётом регистра и пробелов, обязательная единица там, где она нужна, запрет пустых значений и понятное сообщение, если справочник ещё не готов для зависимой сущности.
- `approval required` Защита форм справочников `Оборудование`, `Категории постоянных расходов`, `Ставка труда`: обязательные названия, неотрицательные ставки/стоимости, разумные верхние ограничения или предупреждения для явно ошибочных значений, защита от дублей и подтверждение изменения активной ставки.
- `approval required` Защита критического действия `Сбросить все данные`: многошаговое подтверждение, явное предупреждение о необратимости, блокировка случайного клика и отдельное подтверждение текстом.

Общее правило для всех форм: защита должна дублироваться на уровне сервисов там, где ошибка может нарушить бизнес-инварианты. UI может предотвращать очевидные ошибки и показывать понятные сообщения, но не должен быть единственным источником валидации.

### 5.7. Рецептуры

- `not started` Рецептуры как план, отдельный от фактического производства.
- `not started` Версионирование рецептов.
- `not started` Привязка фактической партии к версии рецепта.
- `not started` Разные базы расчёта рецепта: 1 кг, 10 кг, 100 штук, стандартная партия, произвольная база.
- `later` Автоподстановка ингредиентов при выборе вида заготовки откладывается до появления источника состава/рецептуры.

## 6. Следующий шаг

Следующий шаг: согласовать автоподстановку единицы измерения упаковки на странице `Производство`.

Ожидаемый результат:

- до начала реализации пользователь получает описание предлагаемого UI-поведения и подтверждает именно этот шаг;
- при выборе партии упаковки единица измерения проставляется автоматически;
- ручной выбор единицы упаковки не может создать расхождение с выбранной партией;
- сервисная проверка соответствия партии, упаковки и единицы остаётся источником истины;
- UI-поведение остаётся явным: пользователь выбирает конкретную партию упаковки.

Почему это следующий шаг: отображение стоимости ингредиентов в `Производстве` закрыто. Следующий незакрытый пункт формы производства — автоматическая единица измерения для выбранной упаковки.

## 7. Журнал изменений

- `e717c27` — зафиксировано архитектурное направление.
- `a5ab127` — добавлена сервисная проверка выбранных партий закупок и заготовок.
- `8871166` — добавлены правила сопровождения плана в `AGENTS.md`.
- `92ac080` — добавлена сервисная валидация источников строк закупки.
- `e6b334f` — план реструктурирован в operational roadmap.
- `pending` — добавлены сервисная проверка и UI-выбор выбранной партии готовой продукции в заказах, но работа по заказам отложена до стабилизации предыдущих этапов.
- `pending` — отчёт по движению закупочной партии перенесён в этап отчётов; форма заготовок защищена от базовых ошибок ввода и очищается после успешного сохранения.
- `pending` — добавлен учёт отходов по строкам ингредиентов в заготовках без изменения принципа партийного списания и расчёта себестоимости.
- `pending` — добавлена корректировка неиспользованных заготовок с блокировкой редактирования после использования в производстве.
- `pending` — добавлено удаление неиспользованных заготовок с текстовым подтверждением и сервисной блокировкой после использования в производстве.
- `pending` — улучшен выбор партий ингредиентов в заготовках: сначала выбирается ингредиент, затем конкретная доступная партия.
- `pending` — добавлен в план будущий учёт домашних нужд и домашних запасов как отдельный источник/направление движения партий.
- `pending` — на странице производства стоимость строки ингредиента отображается и пересчитывается по количеству вместо показа редактируемой цены.

## 8. Источники требований

- `README.md`
- `README_FUNCTIONAL.md`
- `01_scope_and_principles.md`
- `02_registers_and_entities.md`
- `03_calculation_rules.md`
- `04_workflows_and_controls.md`
- `AGENTS.md`
